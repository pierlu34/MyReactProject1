import Modal from "../../Modalo/Modal.jsx";

function ShareActivityModal(isOpen, onClose){
    return <Modal isOpen={isOpen} onClose={onClose} header="Condividi Attività">
        <div>CONTENT</div>
        </Modal>
}

export default ShareActivityModal;